{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ReDoS-Radar Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode",
        "${workspaceFolder}"
      ]
    }
  ]
}
