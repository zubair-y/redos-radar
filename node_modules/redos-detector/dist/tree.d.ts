export declare class Tree<T> {
    private decode;
    private root;
    private results;
    private _items;
    constructor(decode: (value: T) => readonly unknown[]);
    add(input: T): void;
    get items(): readonly T[];
}
