import { CharacterReader } from '../character-reader/character-reader-level-0';
import { MyRootNode } from '../parse';
export declare function buildSequenceCharacterReader({ caseInsensitive, dotAll, nodes, }: {
    caseInsensitive: boolean;
    dotAll: boolean;
    nodes: readonly MyRootNode[];
}): CharacterReader;
