import { CharacterReader, CharacterReaderValueGroups } from './character-reader-level-0';
/**
 * Maps each value of a `CharacterReader` from one value to another.
 *
 * It handles the `split` type internally.
 */
export declare function map(reader: CharacterReader, handle: (value: CharacterReaderValueGroups) => CharacterReaderValueGroups): CharacterReader;
